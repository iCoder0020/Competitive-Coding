/*
ID: ishan_sang
PROG: ANTEATER
LANG: C++                  
*/

#include <bits/stdc++.h>

#define int long long

using namespace std;

int32_t main()
{
	int T;
	cin>>T;
	while(T--)
	{
		int N,M;
		cin>>N>>M;
		char temp;
		vector<vector<vector<bool>>> ants(N, vector<vector<bool>>(M, vector<bool>(5, false)));
		for(int n = 0; n<N; n++)
		{
			for(int m = 0; m<M; m++)
			{
				cin>>temp;
				if()
			}
		}
		int cnt = 0;
		for(int i = 0; i<max(N,M); i++)
		{
			for(int n = 0; n<N; n++)
			{
				for(int m = 0; m<M; m++)
				{
					
				}
			}
		}
	}
	return 0;
}
