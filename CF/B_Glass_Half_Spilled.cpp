#include <bits/stdc++.h>
using namespace std;
#define int long long

int32_t main()
{
    int N;
    cin>>N;
    vector<double> A(N), B(N);
    vector<double> ans(N+1, 0);

    for(int k = 1; k<=N; k++)
        cout<<ans[k]<<endl;
    return 0;
}